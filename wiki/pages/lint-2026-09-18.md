---
title: Lint Report 2026-09-18
category: Maintenance
summary: 0 errors, 2 warnings, 7 info — all fixed; mechanical layer clean across 100 pages
tags: [lint, maintenance]
sources: []
created: 2026-09-18
updated: 2026-09-18
---

# Lint Report — 2026-09-18

## Summary
- 🔴 Errors: 0
- 🟡 Warnings: 2 (both contradictions, fixed)
- 🔵 Info: 7 (6 missing cross-references + 1 coverage note, fixed)

Phase 1 (deterministic, whole-wiki) came back completely clean: 0 broken links, 0 missing frontmatter, 0 orphans, 0 slug collisions, 0 stale dates, 0 missing concept pages across 100 pages. Phase 2 dispatched three subagents over the session's recently-edited clusters (metallic/strip, plastic/A005251/tower-spacing, P(k,L)/signed-tower/GF). Clusters not covered by those three were not body-checked this run — the accepted limit of tag-cluster scoping; the recent-edit neighborhood is where fresh issues were most likely.

## 🟡 Contradictions

- [[plastic-number]] said A005251 counts "`n`-bit strings with no isolated `1` (**equivalently**, avoiding the factor `010`)" — but those are *distinct* string families at different offsets (no-isolated-1 length `N` = `A005251(N+2)`; avoid-010 length `N` = `A005251(N+3)`), the very distinction [[a005251-bijection]] is careful to preserve.
  **Fixed:** replaced the false "equivalently" with the correct OEIS reading ("`a(n+3)` = `n`-bit strings avoiding `010`") and a note that the two families are equinumerous only across a one-bit length shift.

- [[metallic-strip-realizability]] carried the "reachable-surd landscape" open bullet **twice**, with divergent field lists — one copy predated the census and framed `Q(√13)` (bronze) as merely "needs four states."
  **Fixed:** removed the stale copy; the surviving bullet points to [[reachable-field-census]] and states the settled result (every real quadratic field is reachable).

## 🔵 Missing Cross-References (all added)

- [[proper-castle-projection]] ↔ [[castle-classification]] — both discuss the Axis-8 `<metal>` growth-castle entities; no mutual link. **Added.**
- [[proper-castle-projection]] ↔ [[pell-castle-strip]] — both discuss the silver/Pell (A000129) rung; no mutual link. **Added.**
- [[hardin-word-identity]] ↔ [[tower-spacing-castles]] — tower-spacing's `g=2` column *is* the Hardin word family; Hardin never linked back. **Added.**
- [[tree-castle-by-area]] ↔ [[tower-spacing-castles]] — the third A005251/`ψ²` node; tree-castle-by-area never linked it. **Added.**
- [[recurrence-discovery]] → [[generating-function-gallery]], [[tower-parity-sectors]], [[oeis-index]] — three inbound-only links (those pages cite recurrence-discovery; it did not reciprocate). **Added.**

## 🔵 Intra-page status tension (fixed)

- [[oeis-index]] tagged the `P(k,·)` rows both **novel-candidate** (in the catalogue, searched 2026-09-18) and **unchecked** (in a stale Legacy bullet). **Fixed:** the Legacy bullet now scopes to only the still-unchecked *k*-direction `|P(k,L)|`, and points to the catalogue for the searched L-direction rows.

## 🔵 Coverage Gaps (from overview)

Most open questions in `overview.md` are internal derivations (convex⟺valley bijection, Viennot heap theory) not answerable by web search or ingest. One is a genuine external action:

- **OEIS submissions** — the verified interlinks and novel-candidate sequences (drafted in `raw/oeis-pe502/`) await human rewording/signing per [[oeis-cross-referencing]]. Not a lint fix; a standing human-action item.

## Clean checks (Phase 2, per subagent special-attention lists)

Verified mutually consistent across their clusters, no findings: all metallic-mean growth constants (`φ`, `1+√2`, bronze `(3+√13)/2`, copper `2+√5=φ³`, nickel `(5+√29)/2`); the `p₁/p₂` notation and metallicity criterion; the `M_h = J − D` rule and its char poly; the A005251 offsets (`tree = A005251(n+2)`, `avoid-010 = A005251(N+3)`, `tower-spacing = A005251(w+3)`, `P_even(6,L)/2^L = A005251(L+3)`) and canonical leading terms; the `P(k,L)` characteristic polynomials, the `den_{k+1} = den_{k−1} − 2x·den_k` recurrence and its `−x ± √(x²+1)` closed form, the coefficient formulas, and the recurrence orders (`k+1` in L, `2L−2` in k).
