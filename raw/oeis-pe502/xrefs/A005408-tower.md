# Cross-link draft — A005408 (odd numbers) as towers of width 2

Status: DRAFT for Charles to reword and submit. OEIS requires human authorship.
Execution notes (field map, signature format, scope): see ../SUBMISSION-NOTES.md.

## Identity (verified)

A005408 = "The odd numbers: a(n) = 2n+1", offset 0, data `1, 3, 5, 7, 9, ...`.

    a(n) = T(2, n) = number of towers of width 2 with exactly n blocks.

## The tower object (self-contained definition)

A **tower** of width w is a stack of unit-height blocks on a line of w cells: each block
is a horizontal segment of adjacent cells at some height, every block rests on the floor or
on the block directly below it (nothing floats or overhangs), and two blocks in the same
row are separated by at least one empty cell.  Equivalently a tower is a column-height
vector (c_1,...,c_w) of nonnegative integers; its number of blocks is the number of
maximal horizontal runs, c_1 + Sum_{i=2..w} max(0, c_i - c_{i-1}).

## What is already there (do NOT duplicate)

A005408 already has dozens of interpretations (Leibniz series, Sharkovskii ordering, cusp
forms, etc.), the closed form `a(n) = 2n+1`, and the g.f. `(1+x)/(1-x)^2`.  It is one of
the densest entries in OEIS — see the Recommendation section below.

## Draft additions

### Comment (append)

> a(n) is also the number of towers of width 2 with n blocks, where a tower is a stack of
> unit-height blocks on a line of cells such that every block rests on the floor or on the
> block below, and two blocks in the same row are separated by at least one empty cell.
> Equivalently, it is the number of pairs of nonnegative integers (c1, c2) with
> max(c1, c2) = n, which is 2n + 1.

### Formula (optional; the closed form is already present)

> a(n) = Sum_{k=1..2} N(2,k) * C(n + 2 - k, 1), where N(2,1) = N(2,2) = 1 are the Narayana
> numbers (A001263); this is (n+1) + n = 2n + 1.

## Recommendation

**Skip this one by default.**  A005408 is one of the densest entries in OEIS (Leibniz,
Sharkovskii ordering, cusp forms, ...) and already contains `a(n) = 2n+1` and the g.f.;
a "number of towers of width 2" comment adds a marginal interpretation to an over-loaded
entry and is the weakest of the tier-2 set.  If you do submit it, add ONLY the Comment
(not the Formula — the closed form is already present).

## Flags

1. Rewrite in own words and sign `- _Charles Reid_, Sep 05 2026` (replace with the actual
   account name and date; see ../SUBMISSION-NOTES.md).
2. If submitted: Comment only, no Formula.  Do not change Name/Data/Offset/Keywords.
